# Minimal config exercising workflow-plugin-turnio.
# Schema-validate with: wfctl validate --skip-unknown-types examples/minimal/config.yaml
version: 1
modules:
  - name: turnio-provider
    type: turnio.provider
    config: {}
workflows:
  pipeline:
    steps:
      - name: example
        type: step.turnio_send_text
        config: {}
